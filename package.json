{
  "name": "ems",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend",
    "shared",
    "scripts"
  ],
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "migrate": "pnpm --filter scripts run migrate",
    "sync:db": "pnpm --filter scripts run sync:db",
    "sync:db:watch": "pnpm --filter scripts run sync:db:watch",
    "sync:all": "pnpm --filter scripts run sync:all"
  },
  "devDependencies": {
    "@types/chokidar": "^2.1.7",
    "chokidar": "^5.0.0",
    "kysely-codegen": "^0.19.0",
    "typescript": "^5.9.3"
  }
}
